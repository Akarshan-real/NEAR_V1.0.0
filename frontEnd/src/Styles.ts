import type { CSSProperties } from "react";

export const dottedBg: CSSProperties = {
    backgroundImage: "radial-gradient(#333 1px, transparent 1px)",
    backgroundSize: "32px 32px",
    backgroundColor: "var(--void)",
}